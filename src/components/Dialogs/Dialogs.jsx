import './Dialogs.scss';

const Dialogs = (props) => {
    return (
        <div>
            <section className="dialogs">
                <h2 className="about__artist">Dialogs</h2>
            </section>
        </div>
    )
}

export default Dialogs